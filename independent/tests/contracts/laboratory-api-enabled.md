Explicit laboratory builds retain the research APIs. This positive counterpart
checks the same module names as the production compile-fail contract.

```rust
use ec_compat::{adapter as _, binary_watch as _, probe as _, protocol_map as _, watch as _};
```
